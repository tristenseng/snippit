---
phase: 01-secure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json",
  "next.config.js",
  "tailwind.config.js",
  "tsconfig.json",
  "src/app/globals.css",
  "src/app/layout.tsx",
  "src/app/page.tsx"
]
autonomous: true
requirements: [AUTH-01, UX-01, UX-02]
user_setup: []

must_haves:
  truths:
    - "Cannabis compliance foundation supports role-based access control"
    - "Mobile-first interface works on 320px viewport with touch targets"
    - "Development environment ready for secure authentication implementation"
  artifacts:
    - path: "package.json"
      provides: "Next.js 15.2.3+ project dependencies"
      contains: "next@15.2.3"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Tailwind CSS"
      min_lines: 15
    - path: "tailwind.config.js"
      provides: "Mobile-first responsive configuration"
      contains: "xs: '320px'"
  key_links:
    - from: "src/app/layout.tsx"
      to: "globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "tailwind.config.js"
      to: "mobile breakpoints"
      via: "responsive configuration"
      pattern: "xs.*320px"
---

<objective>
Establish the Next.js development foundation for the cannabis employee performance tracker with TypeScript, Tailwind CSS, and mobile-first responsive design.

Purpose: Create the secure, compliant infrastructure foundation required for cannabis industry authentication and data privacy before building user-facing features.
Output: Fully configured Next.js development environment ready for database and authentication implementation.
</objective>

<execution_context>
@/Users/Ozora/Desktop/Snippit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Ozora/Desktop/Snippit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-foundation/01-CONTEXT.md
@.planning/phases/01-secure-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with cannabis compliance dependencies</name>
  <files>package.json, next.config.js, tailwind.config.js, tsconfig.json, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - .planning/phases/01-secure-foundation/01-CONTEXT.md (locked technology decisions)
    - .planning/phases/01-secure-foundation/01-RESEARCH.md (exact versions and security requirements)
  </read_first>
  <action>
    Initialize Next.js 15.2.3+ project with App Router and TypeScript. Install exact versions from research:
    
    1. Create Next.js project:
    ```bash
    npx create-next-app@15.2.3 . --typescript --tailwind --app --src-dir --import-alias "@/*"
    ```
    
    2. Install cannabis compliance dependencies:
    ```bash
    npm install next@15.2.3 @auth/prisma-adapter next-auth@4.24.11 prisma@5.22.0 @prisma/client@5.22.0 zod@3.23.8 bcryptjs@2.4.3
    npm install -D @types/bcryptjs @types/node @types/react @types/react-dom
    ```
    
    3. Configure next.config.js for security:
    ```typescript
    const nextConfig = {
      experimental: {
        serverComponentsExternalPackages: ["@prisma/client", "bcryptjs"]
      },
      env: {
        NEXTAUTH_URL: process.env.NEXTAUTH_URL,
        NEXTAUTH_SECRET: process.env.NEXTAUTH_SECRET
      }
    }
    ```
    
    4. Configure Tailwind for mobile-first (320px baseline):
    ```typescript
    module.exports = {
      content: ['./src/**/*.{js,ts,jsx,tsx,mdx}'],
      theme: {
        extend: {
          screens: {
            'xs': '320px',
          },
          minHeight: {
            'touch': '44px'
          }
        }
      }
    }
    ```
    
    5. Update globals.css with mobile-first utilities:
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    
    @layer base {
      html {
        font-size: 16px;
      }
      
      * {
        touch-action: manipulation;
      }
    }
    ```
    
    6. Create root layout with mobile meta tags:
    ```typescript
    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html lang="en">
          <head>
            <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
          </head>
          <body className="min-h-screen bg-gray-50">
            {children}
          </body>
        </html>
      )
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/Ozora/Desktop/Snippit && npm run dev && sleep 5 && curl -I http://localhost:3000 | head -1</automated>
  </verify>
  <done>Next.js application running on localhost:3000 with all cannabis compliance dependencies installed and mobile-first configuration</done>
</task>

</tasks>

<verification>
Development foundation verification:
1. Next.js application serves on localhost:3000
2. Tailwind CSS utilities available for mobile-first design  
3. Cannabis compliance dependencies installed and configured
4. Mobile-responsive foundation established with 320px baseline
</verification>

<success_criteria>
1. Cannabis compliance foundation: Next.js configured with security dependencies for AUTH-01
2. Mobile-first foundation: Tailwind configured for 320px baseline supporting UX-01/UX-02
3. Security foundation: All dependencies installed with latest patches 
4. Development workflow: Next.js development server working
5. Ready for database: Foundation prepared for database integration in next plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-foundation/01-01-SUMMARY.md`
</output>