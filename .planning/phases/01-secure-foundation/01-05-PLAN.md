---
phase: 01-secure-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/auth/RoleSwitcher.tsx
  - src/components/navigation/MobileNav.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "Admin role switcher dropdown shows all role options (Employee, Manager, Admin) fully visible on screen"
    - "Dropdown does not get clipped or overflow past the viewport on mobile"
  artifacts:
    - path: "src/components/auth/RoleSwitcher.tsx"
      provides: "Upward-opening dropdown for role switching"
      contains: "bottom-full"
    - path: "src/components/navigation/MobileNav.tsx"
      provides: "Footer section with overflow-visible for dropdown escape"
      contains: "overflow-visible"
  key_links:
    - from: "src/components/auth/RoleSwitcher.tsx"
      to: "src/components/navigation/MobileNav.tsx"
      via: "RoleSwitcher rendered in MobileNav footer"
      pattern: "<RoleSwitcher"
---

<objective>
Fix the Admin Role Switcher dropdown so all role views (Employee, Manager, Admin) are visible on screen in the mobile navigation panel.

Purpose: UAT Test 8 failed — user reported "you cannot see all the role views. they go past the screen view." The dropdown opens downward in a bottom-flush footer panel, causing it to overflow outside the viewport with no scroll affordance.

Output: RoleSwitcher dropdown opens upward with proper z-index, fully visible on mobile.
</objective>

<execution_context>
@/Users/Ozora/Desktop/Snippit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Ozora/Desktop/Snippit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/auth/RoleSwitcher.tsx
@src/components/navigation/MobileNav.tsx

<interfaces>
<!-- Key components involved in the fix -->

From src/components/auth/RoleSwitcher.tsx (line 41):
```tsx
<div className="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg z-10" role="listbox">
```
Problem: `absolute right-0 mt-2` opens downward, `z-10` is too low.

From src/components/navigation/MobileNav.tsx (line 100-105):
```tsx
<div className="border-t px-4 py-4">
  <RoleSwitcher />
  <div className="mt-4">
    <LogoutButton variant="text" />
  </div>
</div>
```
Problem: Footer div has no overflow-visible, panel clips dropdown.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RoleSwitcher dropdown to open upward with correct z-index</name>
  <files>src/components/auth/RoleSwitcher.tsx, src/components/navigation/MobileNav.tsx</files>
  <action>
In src/components/auth/RoleSwitcher.tsx:
1. Change the dropdown positioning from downward to upward: replace `absolute right-0 mt-2` with `absolute right-0 bottom-full mb-2` on the listbox div (line 41). This makes the dropdown open above the trigger button instead of below it.
2. Increase z-index from `z-10` to `z-[60]` on the same div. The MobileNav overlay uses z-50, so the dropdown must be z-60 to render above it.
3. Also flip the chevron SVG on the trigger button to point upward when the dropdown is open: add a conditional class `${isOpen ? 'rotate-180' : ''}` and add `transition-transform` to the SVG className (line 35). This gives visual feedback that the menu opens upward.

In src/components/navigation/MobileNav.tsx:
4. Add `overflow-visible` to the footer container div (line 100) so the upward-opening dropdown is not clipped by the parent. Change `className="border-t px-4 py-4"` to `className="relative border-t px-4 py-4 overflow-visible"`.
  </action>
  <verify>
    <automated>cd /Users/Ozora/Desktop/Snippit && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>RoleSwitcher dropdown opens upward (bottom-full mb-2), has z-[60], footer has overflow-visible. TypeScript compiles cleanly. All three role options visible without viewport clipping on mobile.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles with no errors: `npx tsc --noEmit`
2. Dev server starts without errors: `npm run dev`
3. Visual check: On mobile viewport (375px), log in as Admin, open hamburger menu, tap role switcher — all 3 roles (Employee, Manager, Admin) appear above the button, fully visible within the screen
</verification>

<success_criteria>
- Admin role switcher dropdown opens upward, showing all 3 role options fully on screen
- Dropdown renders above the MobileNav overlay (z-[60] > z-50)
- No viewport clipping on 375px mobile viewport
- UAT Test 8 passes on re-test
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-foundation/01-05-SUMMARY.md`
</output>
