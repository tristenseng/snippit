---
phase: 02-data-management-core
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/api/batches/[id]/days/[dayId]/entries/route.ts
  - src/app/api/batches/[id]/days/[dayId]/entries/[entryId]/route.ts
  - src/app/api/employees/search/route.ts
  - src/lib/hooks/useDebounce.ts
  - tests/api/entries.test.ts
  - tests/api/employees-search.test.ts
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "Manager can create a weight entry for an employee on a day for a specific strain"
    - "Manager can edit and delete weight entries"
    - "Manager can search employees by first name prefix"
    - "Deactivated employees do not appear in search results"
    - "Employee search is scoped to the batch's location"
  artifacts:
    - path: "src/app/api/batches/[id]/days/[dayId]/entries/route.ts"
      provides: "GET list + POST create weight entry"
      exports: ["GET", "POST"]
    - path: "src/app/api/batches/[id]/days/[dayId]/entries/[entryId]/route.ts"
      provides: "PATCH update + DELETE weight entry"
      exports: ["PATCH", "DELETE"]
    - path: "src/app/api/employees/search/route.ts"
      provides: "GET employee autocomplete search"
      exports: ["GET"]
    - path: "src/lib/hooks/useDebounce.ts"
      provides: "Reusable debounce hook for search inputs"
      exports: ["useDebounce"]
  key_links:
    - from: "src/app/api/batches/[id]/days/[dayId]/entries/route.ts"
      to: "prisma.employeeDay"
      via: "prisma.employeeDay.create"
      pattern: "prisma\\.employeeDay\\.(create|findMany)"
    - from: "src/app/api/employees/search/route.ts"
      to: "prisma.user"
      via: "prisma.user.findMany with deactivatedAt null filter"
      pattern: "deactivatedAt.*null"
---

<objective>
Create weight entry CRUD API routes and employee search endpoint. Weight entries track grams (and optional hours) per employee per day per strain. Employee search filters active employees at a location by first name prefix, supporting the autocomplete component.

Purpose: Server-side data layer for DATA-03 (employee weight input with autocomplete). These routes power the day detail weight entry form in Plan 05.
Output: 3 API route files, useDebounce hook, updated entry and employee search tests.
</objective>

<execution_context>
@/Users/Ozora/Desktop/Snippit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Ozora/Desktop/Snippit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-management-core/02-CONTEXT.md
@.planning/phases/02-data-management-core/02-RESEARCH.md
@.planning/phases/02-data-management-core/02-01-SUMMARY.md

<interfaces>
From prisma/schema.prisma (Plan 01):
```prisma
model EmployeeDay {
  id            String      @id @default(cuid())
  employeeId    String
  dayId         String
  batchStrainId String
  amount        Float
  hours         Float?
  employee      User        @relation(fields: [employeeId], references: [id])
  day           Day         @relation(fields: [dayId], references: [id], onDelete: Cascade)
  batchStrain   BatchStrain @relation(fields: [batchStrainId], references: [id])
  @@map("employees_days")
}

model UserLocation {
  userId     String
  locationId String
  @@id([userId, locationId])
  @@map("user_locations")
}
```

From src/lib/auth.ts:
```typescript
export const authOptions: NextAuthOptions = { ... }
export const auth = () => getServerSession(authOptions)
```

From src/lib/rbac.ts:
```typescript
export const ROLE_PERMISSIONS = {
  MANAGER: { canManageBatches: true, ... },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weight entry CRUD API routes</name>
  <files>src/app/api/batches/[id]/days/[dayId]/entries/route.ts, src/app/api/batches/[id]/days/[dayId]/entries/[entryId]/route.ts, tests/api/entries.test.ts</files>
  <read_first>
    - src/app/api/batches/route.ts (established auth/permission/location-scope pattern — follow same structure)
    - prisma/schema.prisma (EmployeeDay model, BatchStrain model, Day model relations)
    - tests/setup.ts (mock fixtures)
    - tests/api/entries.test.ts (current stub to replace)
  </read_first>
  <behavior>
    - POST /api/batches/[id]/days/[dayId]/entries creates entry with employeeId, batchStrainId, amount, optional hours
    - POST returns 400 if amount <= 0 or amount > 9999.9
    - POST returns 400 if batchStrainId does not belong to the batch
    - GET /api/batches/[id]/days/[dayId]/entries returns all entries with employee name and strain name
    - PATCH /api/batches/[id]/days/[dayId]/entries/[entryId] updates amount and/or hours
    - DELETE /api/batches/[id]/days/[dayId]/entries/[entryId] removes the entry
    - All routes enforce session + canManageBatches
  </behavior>
  <action>
**`src/app/api/batches/[id]/days/[dayId]/entries/route.ts`** — GET list + POST create:

GET handler:
1. Session + canManageBatches check
2. Verify day belongs to batch at manager's location (join through day.batch.locationId)
3. `prisma.employeeDay.findMany({ where: { dayId: params.dayId }, include: { employee: { select: { id: true, name: true } }, batchStrain: { include: { strain: { select: { id: true, name: true } } } } }, orderBy: { employee: { name: 'asc' } } })`
4. Return 200

POST handler:
1. Session + canManageBatches check
2. Zod schema: `z.object({ employeeId: z.string(), batchStrainId: z.string(), amount: z.number().positive().max(9999.9), hours: z.number().positive().max(999.9).optional() })`
3. Verify day belongs to batch at manager's location
4. Verify batchStrainId belongs to the batch (params.id): `prisma.batchStrain.findFirst({ where: { id: batchStrainId, batchId: params.id } })` — 400 if null with `"Strain does not belong to this batch"`
5. `prisma.employeeDay.create({ data: { employeeId, dayId: params.dayId, batchStrainId, amount, hours } })`
6. Return 201 with created entry

**`src/app/api/batches/[id]/days/[dayId]/entries/[entryId]/route.ts`** — PATCH update + DELETE:

PATCH handler:
1. Session + canManageBatches check
2. Zod schema: `z.object({ amount: z.number().positive().max(9999.9).optional(), hours: z.number().positive().max(999.9).nullable().optional() })`
3. Verify entry exists and belongs to day at batch at manager's location
4. `prisma.employeeDay.update({ where: { id: params.entryId }, data: { amount, hours } })`
5. Return 200

DELETE handler:
1. Session + canManageBatches check
2. Verify entry exists and belongs to correct day/batch/location chain
3. `prisma.employeeDay.delete({ where: { id: params.entryId } })`
4. Return 200 with `{ success: true }`

**Update `tests/api/entries.test.ts`** — replace stubs with real tests:
- POST creates entry with valid data, returns 201
- POST returns 400 if amount <= 0 or > 9999.9
- POST returns 400 if batchStrainId does not belong to batch
- GET returns entries with employee name and strain name
- PATCH updates amount and hours
- DELETE removes entry
- All routes return 401 without session, 403 without canManageBatches
  </action>
  <verify>
    <automated>npx jest tests/api/entries.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/batches/[id]/days/[dayId]/entries/route.ts contains `export async function GET`
    - src/app/api/batches/[id]/days/[dayId]/entries/route.ts contains `export async function POST`
    - src/app/api/batches/[id]/days/[dayId]/entries/route.ts contains `z.object` (Zod validation)
    - src/app/api/batches/[id]/days/[dayId]/entries/route.ts contains `batchStrainId`
    - src/app/api/batches/[id]/days/[dayId]/entries/route.ts contains `amount`
    - src/app/api/batches/[id]/days/[dayId]/entries/[entryId]/route.ts contains `export async function PATCH`
    - src/app/api/batches/[id]/days/[dayId]/entries/[entryId]/route.ts contains `export async function DELETE`
    - src/app/api/batches/[id]/days/[dayId]/entries/[entryId]/route.ts contains `prisma.employeeDay.delete`
    - tests/api/entries.test.ts contains `201` (success test)
    - tests/api/entries.test.ts contains `400` (validation test)
    - tests/api/entries.test.ts does NOT contain `expect(true).toBe(true)` (stubs replaced)
    - `npx jest tests/api/entries.test.ts --no-coverage` exits 0
  </acceptance_criteria>
  <done>Weight entry CRUD routes create, list, update, and delete entries with validation that strain belongs to batch, amount is positive, and location scoping is enforced</done>
</task>

<task type="auto">
  <name>Task 2: Employee search API and useDebounce hook</name>
  <files>src/app/api/employees/search/route.ts, src/lib/hooks/useDebounce.ts, tests/api/employees-search.test.ts</files>
  <read_first>
    - prisma/schema.prisma (User model with deactivatedAt, UserLocation join table)
    - src/lib/auth.ts (session pattern)
    - tests/setup.ts (mock fixtures)
    - tests/api/employees-search.test.ts (current stub to replace)
  </read_first>
  <behavior>
    - GET /api/employees/search?locationId=xxx returns active employees at location
    - Deactivated employees (deactivatedAt IS NOT NULL) are excluded
    - Results include id and name fields only
    - Results ordered by name ascending
    - Returns 400 if locationId query param missing
    - Returns 401 without session
  </behavior>
  <action>
**`src/app/api/employees/search/route.ts`** — GET:

1. `getServerSession(authOptions)` — 401 if null
2. Read `locationId` from `request.nextUrl.searchParams.get('locationId')` — 400 if missing
3. Query: `prisma.user.findMany({ where: { deactivatedAt: null, role: 'EMPLOYEE', userLocations: { some: { locationId } } }, select: { id: true, name: true }, orderBy: { name: 'asc' } })`
4. Return 200 with employee array

Note: No canManageBatches check here — managers need this for weight entry, but the data returned (id + name) is not sensitive. Session auth is sufficient.

**`src/lib/hooks/useDebounce.ts`** — client-side debounce hook:

```typescript
'use client'
import { useState, useEffect } from 'react'

export function useDebounce<T>(value: T, delay: number): T {
  const [debouncedValue, setDebouncedValue] = useState<T>(value)

  useEffect(() => {
    const timer = setTimeout(() => setDebouncedValue(value), delay)
    return () => clearTimeout(timer)
  }, [value, delay])

  return debouncedValue
}
```

**Update `tests/api/employees-search.test.ts`** — replace stubs:
- Test that deactivated employees are excluded (mock prisma.user.findMany to verify where clause includes `deactivatedAt: null`)
- Test that locationId filter is applied via userLocations.some
- Test 400 when locationId missing
- Test 401 when no session
  </action>
  <verify>
    <automated>npx jest tests/api/employees-search.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/employees/search/route.ts contains `export async function GET`
    - src/app/api/employees/search/route.ts contains `deactivatedAt: null`
    - src/app/api/employees/search/route.ts contains `userLocations`
    - src/app/api/employees/search/route.ts contains `role: 'EMPLOYEE'` or `role: "EMPLOYEE"`
    - src/lib/hooks/useDebounce.ts contains `export function useDebounce`
    - src/lib/hooks/useDebounce.ts contains `'use client'`
    - src/lib/hooks/useDebounce.ts contains `setTimeout`
    - tests/api/employees-search.test.ts contains `deactivatedAt`
    - tests/api/employees-search.test.ts does NOT contain `expect(true).toBe(true)`
    - `npx jest tests/api/employees-search.test.ts --no-coverage` exits 0
  </acceptance_criteria>
  <done>Employee search API excludes deactivated employees, filters by location via userLocations join, returns id+name ordered by name. useDebounce hook ready for autocomplete component.</done>
</task>

</tasks>

<verification>
- `npx jest tests/api/entries.test.ts tests/api/employees-search.test.ts --no-coverage` — all pass
- Employee search endpoint excludes deactivated users
- Weight entry routes validate strain belongs to batch
- All routes enforce session authentication
</verification>

<success_criteria>
- Weight entry CRUD (create, list, update, delete) working with validation
- Employee search excludes deactivated employees and filters by location
- useDebounce hook exported as reusable client hook
- Tests cover deactivation filtering, validation edge cases, and entry CRUD
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-management-core/02-03-SUMMARY.md`
</output>
