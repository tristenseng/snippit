---
phase: 02-data-management-core
plan: 07
type: execute
wave: 4
depends_on: ["02-05", "02-06"]
files_modified: []
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - ADMIN-01

must_haves:
  truths:
    - "Manager can create batch, add day, enter weights, submit day — full workflow"
    - "Admin can create account, edit account, deactivate account — full workflow"
    - "forcePasswordReset redirects new account to set-password page"
    - "Deactivated employees do not appear in autocomplete search"
    - "All pages render correctly on mobile (320px min width)"
  artifacts:
    - path: "src/app/(dashboard)/batches/page.tsx"
      provides: "Batch list page renders at 320px without horizontal overflow"
    - path: "src/app/(dashboard)/batches/[id]/days/[dayId]/page.tsx"
      provides: "Day detail page with functional weight entry form"
    - path: "src/app/(dashboard)/admin/users/page.tsx"
      provides: "Admin user table switches to card layout at 320px"
    - path: "src/app/set-password/page.tsx"
      provides: "Set password page accessible during forcePasswordReset flow"
  key_links:
    - from: "src/components/days/EmployeeAutocomplete.tsx"
      to: "/api/employees/search"
      via: "fetch excludes deactivated employees"
      pattern: "deactivatedAt.*null"
    - from: "src/middleware.ts"
      to: "/set-password"
      via: "redirect on forcePasswordReset=true"
      pattern: "forcePasswordReset"
---

<objective>
Visual and functional verification of the complete Phase 2 data management workflow. Manager batch/day/weight-entry flow and admin user management flow tested end-to-end in browser.

Purpose: Catch integration issues, visual regressions, and workflow gaps before marking Phase 2 complete.
Output: Verification sign-off or issue list for gap closure.
</objective>

<execution_context>
@/Users/Ozora/Desktop/Snippit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Ozora/Desktop/Snippit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-management-core/02-CONTEXT.md
@.planning/phases/02-data-management-core/02-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end visual and functional verification of Phase 2</name>
  <files></files>
  <action>
Present the verification checklist to the user and wait for their approval or issue report. Ensure the dev server is running before presenting the checklist. Run `npm run dev` if not already running.
  </action>
  <verify>User confirms "approved" or provides issue list</verify>
  <done>All 6 test scenarios pass in browser, or issues are documented for gap closure</done>
  <what-built>Complete Phase 2 data management system: manager batch/day/weight-entry workflow and admin user management with forcePasswordReset</what-built>
  <how-to-verify>
**Prerequisites:** Ensure dev server is running (`npm run dev`). You need at least one Location and one Strain in the database. If needed, create via Prisma Studio (`npx prisma studio`) or seed script.

**Test 1: Manager Batch Workflow (DATA-01, DATA-02)**
1. Log in as a MANAGER user
2. Navigate to `/batches`
3. Verify page title "Batch Management" and "Create Batch" button visible
4. Click "Create Batch" — verify strain selection form appears
5. Select at least one strain, submit
6. Verify new batch card appears with INACTIVE status badge (yellow)
7. Click batch card to view detail
8. Click "Activate" — verify status changes to ACTIVE (green)
9. Click "Add Day" — verify Day 1 is created

**Test 2: Weight Entry (DATA-03)**
1. Click into Day 1
2. Select a strain from the dropdown
3. Type an employee's first name in the autocomplete
4. Verify dropdown appears with matching employees
5. Select an employee, enter grams (e.g., 125.5)
6. Click "Add Entry" — verify entry appears in list below
7. Click edit icon on entry — verify inline edit works
8. Click delete icon — verify "Remove?" confirmation appears
9. Click "Yes, Remove" — verify entry is deleted

**Test 3: Day Submission (DATA-04)**
1. Add at least one weight entry to the day
2. Click "Submit Day"
3. Verify inline confirmation: "Submit Day 1? Employees will see this data."
4. Click "Submit Day" confirmation
5. Verify day shows "Submitted" badge (green)

**Test 4: Admin User Management (ADMIN-01)**
1. Log in as ADMIN (or switch role if admin)
2. Navigate to `/admin/users`
3. Verify "Admin Panel" title and "Add Employee" button
4. Click "Add Employee" — verify modal opens
5. Fill in: name, email, temporary password, role, location(s)
6. Submit — verify success message and modal closes
7. Verify new user appears in table
8. Click "Edit" on a user — verify edit page shows with email disabled (grayed out)
9. Change role, save — verify update
10. Click "Deactivate" on a user — verify confirmation dialog with correct text
11. Confirm deactivation — verify "Deactivated" badge on row

**Test 5: forcePasswordReset Flow**
1. Log out
2. Log in with the newly created account's temporary password
3. Verify redirect to `/set-password` (NOT dashboard)
4. Set a new password (min 8 chars, must match confirmation)
5. Verify redirect to `/dashboard` after password set
6. Log out and log back in with new password — verify normal access

**Test 6: Mobile Responsiveness**
1. Open Chrome DevTools, set viewport to 320px width
2. Check `/batches` — batch cards should stack vertically
3. Check day detail — form should be full width
4. Check `/admin/users` — table should switch to stacked card layout
5. Check all touch targets are at least 44px tall
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 6 test scenarios pass in browser testing.
</verification>

<success_criteria>
- Manager batch/day/weight workflow functional end-to-end
- Admin user management CRUD functional
- forcePasswordReset flow works on first login
- Deactivated employees excluded from autocomplete
- Mobile responsive at 320px
- All copywriting matches UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-management-core/02-07-SUMMARY.md`
</output>
